module tb_full_adder_struct;
    reg a, b, cin;
    wire sum, cout;

    full_adder_struct dut(a, b, cin, sum, cout);

    initial begin
        $dumpfile("full_adder_struct.vcd");
        $dumpvars(0, tb_full_adder_struct);
        for (integer i = 0; i < 8; i = i + 1) begin
            {a, b, cin} = i;
            #10;
        end
        $finish;
    end
endmodule
